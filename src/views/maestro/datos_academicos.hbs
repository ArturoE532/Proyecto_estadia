<div class="container">
    <div class="row">
        <div class="col-4"></div>
        <div class="col-8"><a href="javascript:history.back()" class="btn btn-danger m-3 ">DATOS ACADEMICOS</a></div>
        <div class="col-4"></div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-4"></div>
        <div class="col-8"><a href="javascript:history.back()" class="btn btn-danger m-3 ">DATOS ACADEMICOS</a></div>
        <div class="col-4"></div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-4"></div>
        <div class="col-8"><a href="javascript:history.back()" class="btn btn-danger m-3 ">Agregar</a>
         <a href="/imp" class="btn btn-danger btn-block m-3">Imprimir Curriculum</a></div>
        <div class="col-4"></div>
    </div>
</div>


<table class="table table-dark table-striped">

    <thead>
        <tr>
           
            <th scope="col">Titulo</th>
            <th scope="col">Nivel de escolaridad</th>
            <th scope="col">Estatus</th>
            <th scope="col">Pais</th>
            <th scope="col">Fecha de obtencion</th>
            <th scope="col">Acciones</th>
        </tr>
    </thead>

    <tbody>

        {{#each user}}

        <tr>
            <th scope="row">{{@index}}</th>
            <td>
                <input type="text" class="form-control" name="title" value="{{name}}" readonly>
            </td>
            <td>
                <input type="text" class="form-control" name="title" value="{{createdAt}}" readonly>
            </td>
            <td>{{#if status}}
                    <input type="text" class="form-control" name="title" value="Activo" readonly>
                {{else}}
                    <input type="text" class="form-control" name="title" value="Inactivo" readonly>
                {{/if}}
            </td>
            <td>

                <a href="/personal/info/{{_id}}" class="btn btn-primary btn-block btn-sm">Ver perfil</a>
               
            </td>
               <td>

                <a href="/personal/info/{{_id}}" class="btn btn-primary btn-block btn-sm">Editar</a>
               
                {{#if status}}
                    <a href="/personal/toogleStatus/{{_id}}" class="btn btn-danger btn-block btn-sm">Eliminar</a>
                {{else}}
                    <a href="/personal/toogleStatus/{{_id}}" class="btn btn-success btn-block btn-sm">Eliminar</a>
                {{/if}}
            </td>
        </tr>

        {{/each}}

    </tbody>

</table>